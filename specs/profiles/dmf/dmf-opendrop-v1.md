# DMF OpenDrop Profile v1

OpenDrop-like behavior is treated as a DMF profile variant. Hardware-specific calibration,
addressing, tolerances, and recovery strategies belong here rather than in Klein Core.
