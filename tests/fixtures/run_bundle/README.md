# Run Bundle Fixtures

These fixtures exercise KCP Run Bundle v1 as a transport artifact. The signed run manifest remains the cryptographic claim object.

The keys are public test fixtures only. Trust means authorized by the fixture Trust Policy v1 document only; no hardware attestation, trusted timestamp, physical truth, or substrate attestation is implied.
